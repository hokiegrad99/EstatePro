{
  "name": "functions",
  "description": "Cloud Functions for EstatePro (Phase 4 demo helpers + the existing createInviteLink/retireBootstrapLock callable pair).",
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "firebase-admin": "^11.11.0",
    "firebase-functions": "^4.9.0"
  },
  "private": true
}
